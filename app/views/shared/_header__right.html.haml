.header__right
  .header__right--title
    super
  .header__right--text
    Member : 
  .header__right--list
    asd kamiya-s-max
  - current_user.groups.each do |group|
    =link_to edit_group_path(group), class: "header__right--btn" do
      Edit